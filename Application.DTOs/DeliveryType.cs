//using System.Runtime.Serialization;

namespace Application.DTOs
{
    public enum DeliveryType
    {
        //[EnumMember(Value="0")] If you need Jil to render this enum as string number
        OneWeek = 0,
        SameDay = 1,
        Premium = 2
    }
}